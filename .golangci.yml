version: "2"
linters:
  default: fast
  enable:
    - staticcheck
    - gomodguard_v2
    - wsl_v5
  disable:
    - gomodguard
    - depguard
    - mnd
    - prealloc
    - testpackage
    - wsl
  exclusions:
    warn-unused: true
    rules:
      - path: _test\.go
        linters:
          - funlen
formatters:
  enable:
    - gofmt
    - gofumpt
    - golines
